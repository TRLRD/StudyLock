name: Build Windows EXE

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Syntax check
        run: python -m py_compile main.py test_studylock.py

      - name: Unit tests
        run: python -m unittest test_studylock.py -v

      - name: Build EXE
        run: pyinstaller --noconfirm --clean --onefile --windowed --name StudyLock main.py

      - name: Upload EXE artifact
        uses: actions/upload-artifact@v4
        with:
          name: StudyLock-Windows
          path: dist/StudyLock.exe
